# 推箱子的领域语言

玩家通过移动并推动箱子，将全部箱子放到目标点。初始布局、编辑草稿与一次游玩的状态彼此独立。

## 游玩

**移动意图**：
玩家要求向上、右、下或左移动一格的操作，不保证实际移动成功。
_Avoid_: 有效步

**棋盘位置**：
玩家或箱子实际占据的格子。移动反馈中的中间画面不构成新的棋盘位置。

**移动反馈**：
对一次移动结果的画面或声音提示，包括走动、推动及受阻反馈；它不决定移动是否合法。

**游玩状态**：
一次游戏中玩家与箱子的棋盘位置，以及步数、推动次数和撤销历史。
_Avoid_: 初始布局、编辑草稿

**有效步**：
玩家成功移动到相邻格的一次操作。推动箱子同时计一个有效步，受阻不计步。

**推动**：
玩家把相邻箱子推入前方空格，同时进入箱子原来的位置。一次只能推动一个箱子，不能拉箱子。

**目标点**：
箱子需要到达的位置。它独立于玩家和箱子，两者离开后仍存在。

**通关**：
全部箱子位于目标点的状态。

**正式成绩**：
同一关卡身份、同一布局版本对应的通关记录。旧布局的成绩不用于新布局。

## 关卡

**关卡定义**：
一关的初始布局与资料，包括稳定身份、布局版本、名称、说明、尺寸、地形、目标点及玩家和箱子的起点。

**关卡资产**：
工程中持久保存的关卡定义及可选验证解。它与尚未保存的编辑草稿有区别。

**草稿关卡**：
已保存但未收录到正式关卡目录的关卡，可以具有尚未完成的结构。

**正式关卡**：
已收录到关卡目录、可以从选关列表进入的关卡。覆盖保存前必须通过结构校验。

**关卡目录**：
正式关卡的有序列表。顺序决定导航而非身份；从目录移除一关不等于删除关卡资产。

**布局版本**：
同一关卡初始布局的修订号。保存实际布局变化才递增，仅修改名称、说明或翻译不递增。

**结构校验**：
检查数量、边界、地形和占用是否合法，不证明关卡可解。

**验证解**：
开发期间已验证能使关卡通关的一组方向操作。布局变化后失效；缺少验证解不意味着应恢复旧布局。

## 编辑

**关卡策划**：
在开发过程中制作、跑测和调整关卡的使用者，不要求具备编程能力。

**运行时编辑**：
在游戏运行期间切换到关卡制作的工作方式。本项目面向开发人员，不表示向普通玩家开放创作。

**编辑草稿**：
正在修改的初始布局工作副本。绘制、撤销、预览和试玩均不等于保存关卡资产。
_Avoid_: 游玩状态、草稿关卡

**元素**：
决定绘制什么的内容类别，包括地板、墙、目标点、玩家和箱子。目标点与对象可以共处一格，玩家与箱子不能共处一格。

**工具**：
决定怎样编辑的操作方式，包括画笔、直线、矩形、框选、填充和橡皮。

**完整格子内容**：
一格的地形、目标点和对象占用的组合。填充只扩散到四方向相连且完整内容相同的格子。

**选区**：
包含边界的格子矩形，其内容包含空地。移动整块选区会把原区域恢复为空地，区域越界时整次操作被拒绝。

**选区快照**：
选区在一次操作前的完整格子内容。重叠移动和粘贴依据这一份内容完成，避免源区域变化影响操作结果。

**选区复制**：
复制选区内容供粘贴使用，其中跳过玩家、保留玩家脚下的地形和目标点。粘贴覆盖目标区域的完整内容，包括空地；会覆盖原玩家的粘贴不被允许。

**对象拖动**：
只移动玩家或箱子，保留原格与目标格的地形和目标点。目标墙面或对象占用会阻止拖动。

**编辑事务**：
一次完整手势或批量操作，对应一次撤销。取消预览或拒绝操作不留下部分修改。

**保存**：
把编辑草稿明确写入关卡资产的操作。覆盖保存保持关卡身份，另存为创建新身份并从初始布局版本开始。

## 预览与试玩

**已保存关卡预览**：
在不开始游戏的情况下查看已保存关卡，不产生游玩状态、成绩或布局修改。

**草稿预览**：
查看编辑中尚未保存的布局，或查看待提交操作的结果。取消待提交操作不会修改草稿。

**试玩**：
用当前布局副本进行的独立游戏，使用正式游戏规则，但不产生正式成绩。

**返回编辑**：
结束独立试玩并恢复原草稿、编辑历史和视角。

**返回游戏**：
结束编辑并恢复原游戏。若原关卡已保存新布局，则使用新布局重新开始，避免把旧游玩状态算入新版本成绩。
